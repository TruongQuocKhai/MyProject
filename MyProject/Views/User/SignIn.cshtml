@model MyProject.Models.SignInModel
@{
    ViewBag.Title = "Đăng Nhập";
    Layout = "~/Views/Shared/_LayoutUser.cshtml";
}
@section jsFooter {
    <script src="~/Contents/js/show-hide-password.js"></script>
}
<style>
    .field-icon {
        float: right;
        margin-left: -25px;
        margin-top: -32px;
        position: relative;
        z-index: 2;
    }
</style>

<section class="padding-small">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-6">
                <div class="block">
                    <div class="block-header">
                        <h5>Đăng nhập</h5>
                    </div>
                    <div class="block-body">
                        <div class="lead">
                            Nếu bạn chưa có tài khoản click <a href="/dang-ky" style="font-weight:bold; font-style: italic;">vào đây</a> để đăng ký
                        </div>
                        <hr>
                        <form action="@Url.Action("SignIn","User")" method="post">
                            @Html.ValidationSummary(false, "", new { @class = "text-danger" })
                            @Html.AntiForgeryToken()
                            <div class="form-group">
                                <label for="text" class="form-label">Địa chỉ email</label>
                                <input id="Email" name="Email" type="email" class="form-control" placeholder="Nhập tên tài khoản hoặc email hoặc số điện thoại">
                                @Html.ValidationMessage("Email", new { @class = "text-danger" })
                            </div>
                            <div class="form-group">
                                <label for="password" class="form-label">Mật khẩu</label>
                                <input id="password-field" name="Password" type="password" value="" placeholder="Nhập mật khẩu" toggle="#password-field" class="fa fa-fw fa-eye field-icon toggle-password form-control" style="position:initial;">
                                @Html.ValidationMessage("Password", new { @class = "text-danger" })
                            </div>
                            <div class="form-group text-center">
                                <button type="submit" class="btn btn-primary"><i class="fa fa-sign-in"></i>Đăng Nhập</button>
                            </div>
                            @*@if (Session[CommonConstantUser.MESSAGE_USER] != null)
                                {
                                    <div class="alert alert-danger" role="alert">
                                        @Session[CommonConstantUser.MESSAGE_USER]
                                    </div>
                                }*@
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>